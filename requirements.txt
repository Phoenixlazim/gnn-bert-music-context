numpy>=1.24
scipy>=1.10
PyYAML>=6.0
librosa>=0.10.1
soundfile>=0.12
audioread>=3.0
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
tqdm>=4.65

torch>=2.1
transformers>=4.35

# PyTorch Geometric — install AFTER torch, matching your CUDA build:
#   pip install torch_geometric
# If the RGAT layer errors on scatter ops, add the compiled extras:
#   pip install pyg_lib torch_scatter torch_sparse \
#     -f https://data.pyg.org/whl/torch-$(python -c "import torch;print(torch.__version__)").html
torch_geometric>=2.4
